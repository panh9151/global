import { Twig } from 'twig'
import { ResolvedConfig } from 'vite'

export default (Twig: Twig, config: ResolvedConfig) => {
  // Functions
  // Twig.exports.extendFunction('repeat', function (value, times) {
  //   return new Array(times + 1).join(value)
  // })

  // Filters

  // Tags
}
